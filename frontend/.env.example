# Local development can use /v1 through the Vite proxy.
# Production should use the complete API URL, including the /v1 prefix.
VITE_API_BASE_URL=/v1
VITE_CLERK_PUBLISHABLE_KEY=
VITE_RUNR_ACCESS_TOKEN=
