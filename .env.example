CLERK_SECRET_KEY=
CLERK_PUBLISHABLE_KEY=
CLERK_WEBHOOK_SECRET=

LEMONSQUEEZY_API_KEY=
LEMONSQUEEZY_WEBHOOK_SECRET=
LEMONSQUEEZY_STORE_ID=

# Frontend build configuration.
# Local Vite development uses frontend/.env.local with VITE_API_BASE_URL=/v1.
VITE_API_BASE_URL=https://yourdomain.com
VITE_CLERK_PUBLISHABLE_KEY=

# Production only
BACKEND_ALLOWED_ORIGINS=https://yourdomain.com
PORT=3000

# Optional: enables live public-web + DeepSeek target-contact discovery.
# Leave unset for deterministic local/test behavior.
RUNR_ENABLE_LIVE_NETWORKING_DISCOVERY=

# Clerk session token template expected by the frontend/backend integration:
# Name: runr_backend
# Claims:
# {
#   "publicMetadata": {
#     "role": "{{user.public_metadata.role}}",
#     "plan_id": "{{user.public_metadata.plan_id}}",
#     "quota_overrides": "{{user.public_metadata.quota_overrides}}"
#   }
# }
