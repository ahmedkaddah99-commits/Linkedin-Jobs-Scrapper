# AA-225 CI end-to-end evidence — PASS

Date: 2026-08-01
Branch: `deployment/render-turso-r2`
Prerequisite: AA-P03 PASS, commit `861f5f8`

## Scope and environment

This is a controlled, sanitized-fixture CI gate. No live employer form was
opened, no candidate values were logged, and no production feature flag was
enabled. Local environment: Python 3.12.7 from `.venv\\Scripts\\python.exe`,
Node v24.11.0, npm v11.6.1, Chromium Playwright fixture server, and the
repository's testing-mode MV3 extension build.

## Coverage matrix

| Requirement | Browser/test evidence | Result |
|---|---|---|
| Greenhouse and Lever inactive start through exact-tab review | `tests/e2e/aa201-inactive-fixture.spec.ts` | PASS; both ATS fixtures, inactive before activation, exact local tab activation |
| Job-specific provenance and approved content | AA-211 package/tailored-document tests and package-backed Playwright cases | PASS; IDs/hashes remain package data and no browser-side rewriting |
| Repeatable experience/education reconciliation | `tests/e2e/aa202-reconciliation.spec.ts`; reconciliation unit tests | PASS; rerun/reload/remount, promotions, overlaps, unmatched preservation, ambiguity |
| Complex controls | `tests/e2e/assisted-apply.spec.ts:406-503`; AA-218 unit tests | PASS; native, controlled, frames, open roots; unsupported boundaries unresolved |
| Document grants and uploads | AA-221 unit/upload tests and `assisted-apply.spec.ts:722-918` | PASS; exact role, hash/version, multiple roles, wrong-field/ambiguous denial |
| Permission and sidepanel lifecycle | AA-214/215/222 unit tests and sidepanel Playwright lifecycle test | PASS; permission required, suspension/reload/retry/close states |
| Sensitive and ambiguous questions | AA-219/220 adapter tests, package precedence tests, fixture review assertions | PASS; surfaced unresolved, never guessed |
| Forged/replayed commands | AA-210/214 message and replay tests; extension message validation | PASS; invalid origin, stale, replayed, malformed, wrong association fail closed |
| Sanitized telemetry | AA-15 telemetry tests, upload telemetry assertions, boundary searches | PASS; aggregate counts/categories only; no raw candidate values |
| Every submission pathway | submission guard, manifest/boundary scripts, Playwright instrumentation | PASS; submit/requestSubmit/form.submit/Enter/terminal click/request/success/navigation all remained zero |

## Commands and results

- `.venv\\Scripts\\python.exe --version` → Python 3.12.7.
- `npm --prefix apps/browser-extension run test:unit` → **23 files, 183
  passed**.
- `npm --prefix apps/browser-extension run typecheck` → passed.
- `npm --prefix apps/browser-extension run verify:manifest` → passed.
- `npm --prefix apps/browser-extension run verify:assisted-apply-boundary` →
  passed.
- `npm --prefix apps/browser-extension run test:e2e` → **22 passed**; no
  retry was required and no trace was produced for a failure.
- `npm --prefix frontend run check` → **107 passed**, ESLint passed, Vite
  production build passed.
- `.venv\\Scripts\\python.exe -m pytest -q tests/test_backend_api.py` →
  **121 passed, 14 subtests passed** in 234.22s.
- `.venv\\Scripts\\python.exe -m pytest -q <all root test files except
  tests/test_backend_api.py>` → **965 passed, 61 subtests passed** in 129.20s.
- `.venv\\Scripts\\python.exe -m ruff check backend tests` → passed.
- `git diff --check` → passed for the committed gate/workflow changes.

The API/non-API split is encoded in `.github/workflows/ci.yml` as the
`backend-full` matrix. Extension CI runs `check:all`, installs Chromium, and
uploads Playwright traces/report artifacts only on failure. The feature remains
disabled by default through the existing frontend/backend gates.

## Limitations and explicit follow-up

This gate proves sanitized browser fixtures and deterministic CI behavior. It
does not authorize a live pilot, broad host access, or production enablement.
AA-226 must separately provide the required controlled live-form sample and
sanitized field-level measurements. Any reproducible live failure must first
become a sanitized fixture/regression test.

## Verdict

**AA-225: PASS.** The controlled CI workflow is repeatable and the supported
fixture scope meets the no-duplicate, no-submission, forged-command, and
sanitized-telemetry requirements. AA-226 may proceed under its separate pilot
controls.
