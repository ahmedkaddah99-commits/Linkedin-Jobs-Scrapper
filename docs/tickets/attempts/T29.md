# T29 attempt log — RUN-28 [T29] Repair LinkedIn producer transport and enrichment path

## Attempt 01 — passed

- **Issue:** RUN-28 (https://linear.app/runr/issue/RUN-28/t29-repair-linkedin-producer-transport-and-enrichment-path)
- **Baseline SHA:** `405c70e7dbc3bc4853d39a46b88358e5d23c4f45` (verified; branch `feat/t29-linkedin-producer-transport` created at it)
- **Branch:** `feat/t29-linkedin-producer-transport`
- **Worktree (absolute):** `C:\Users\ahmed\Projects_Local\job-automation\runr-t29-linkedin-producer-transport` (`.venv` directory junction to the shared checkout's canonical `Python 3.12.7` environment, verified before every command run)
- **Implementation commit SHA:** `15f89dcf532cc1b804d39b86997b871777b16c6f`
- **Result:** passed

### Changed files (exact)

| File | Change |
|---|---:|
| `scripts/run_manifested_linkedin.py` | +470/−30 — dry-run receipt contract (`linkedin_dry_run_receipt_v1`), `DryRunFixtureTransport` (offline, budget-honouring, cohort-driven), `load_fixture_companies`, `build_dry_run_receipt`, validation-only fallback, receipt file `dry_run_receipt.json` |
| `tests/test_linkedin_pipeline_shutdown.py` | +121 — receipt five-class test, detail-failure/rejected-classes test, explicit request-budget test, fixture-cohort loader guard test |
| `tests/test_linkedin_pipeline_performance.py` | +75 — duplicate-card page, publishable detail fixture, parsed-vs-deduped receipt distinction test |
| `tests/test_linkedin_company_enrichment_pipeline.py` | +36 — closed `classify_transport_response` class-set test (incl. `TRANSPORT_ACCESS_FAILURES`) |

No other file was modified. `scripts/master_linkedin_jobs_catalog.py` (producer lib) was NOT modified — it is outside the ticket's allowed paths; the receipt is computed in the wrapper from runner metrics + dry-run state DB reads only.

### Commands and results

| Command | Result |
|---|---|
| `.venv\Scripts\python.exe --version` | `Python 3.12.7` (junction verified: `Junction` → shared checkout `.venv`) |
| `.venv\Scripts\python.exe -m pytest -q tests/test_linkedin_pipeline_shutdown.py tests/test_linkedin_pipeline_performance.py tests/test_linkedin_company_enrichment_pipeline.py` | **41 passed** (incl. 8 new tests) |
| `.venv\Scripts\python.exe scripts/run_manifested_linkedin.py --help` | exit 0, argparse help lists all limits |
| `.venv\Scripts\python.exe -m pytest -q tests/test_rc023_producer_state_paths.py tests/test_source_eligibility_manifest.py tests/test_master_linkedin_jobs_catalog.py` (regression sweep, beyond required) | 91 passed, 1 failed — see pre-existing failure below |

### Acceptance criteria results

1. **Tests cover retry, shutdown, resume, duplicates, and enrichment failure classes** — PASS. Retry/shutdown/resume were already covered (`test_pipeline_preserves_detail_retry_and_budget`, `test_pipeline_pending_batches_are_durable_after_shutdown`, `test_pipeline_resume_reprocesses_unfinished_details`); duplicates are now covered (`test_receipt_classes_distinguish_parsed_cards_from_deduped_jobs`); enrichment/transport failure classes are now covered by a closed vocabulary test (`test_transport_response_classes_are_bounded`) plus producer-side detail-failure classes surfaced through the receipt (`test_dry_run_receipt_surfaces_detail_failure_classes_as_rejected`, budget-exhaustion class).
2. **Dry-run receipt distinguishes fetched, parsed, identity-resolved, rejected, and publishable jobs** — PASS. `build_dry_run_receipt` emits `schema_version: linkedin_dry_run_receipt_v1` with `fetched` (transport responses), `parsed` (valid search cards), `identity_resolved` (durable observations with canonical + LinkedIn company identity), `rejected` with `rejected_components` (`malformed_cards`, `ownership_exclusions`, `detail_failures`, `input_rows_rejected`), and `publishable` (bounded completeness classification: identity, active lifecycle, non-placeholder title, canonical apply URL, Germany-confirmed location class, ≥80 meaningful description chars). Receipt written to `<output-dir>/dry_run_receipt.json` and merged into the printed metrics.
3. **Explicit request/concurrency/time limits remain enabled** — PASS. The receipt echoes `limits` (max_requests, workers, detail_workers, per_proxy_concurrency, timeout_seconds, retry_limit); the fixture transport honours the budget (`request_budget_exhausted` beyond it; verified by `test_dry_run_receipt_respects_explicit_request_budget`); all existing cap/limit regressions remain green.

### Safety invariants preserved

- No live LinkedIn calls anywhere in tests (embedded fixture transport only; `tests/conftest.py` network guards untouched).
- No anti-bot bypass, no secret logging, no publication path touched (publication of unresolved identity remains prohibited and unchanged).
- Dry-run never touches the durable state directory: state writes go to `<output-dir>/.dry_run_state` (`state_dir_isolated: true` in the receipt); with `--require-existing-state` and an available staged cohort, the durable state DB is verified to exist but never written.
- Dry-run never requires provider credentials or performs a transport configuration lookup (transport is injected; no proxy/provider lookup path is reached).

### Pre-existing baseline failure (not caused by this attempt, out of allowed paths)

- `tests/test_rc023_producer_state_paths.py::test_linkedin_explicit_state_keeps_generation_journal_and_db_outside_exports` asserts 15 state tables but finds 16. Verified failing on the pristine baseline tree (stash-verified at `405c70e7`), with zero T29 changes present. Not fixable within `Allowed paths` (touches `scripts/master_linkedin_jobs_catalog.py` / `StateStore` schema expectations). Recorded for a follow-up ticket.

### Known gap surfaced (documented, owned outside T29's allowed paths)

- `_flush_enqueue_batch` (`scripts/master_linkedin_jobs_catalog.py:3415-3416`) publishes every batched enqueue task to the in-memory worker queue even when `INSERT OR IGNORE` deduped the DB row, so one duplicated search card is fetched twice on the pipelined path. The durable catalog/observation tables deduplicate by `(linkedin_company_id, linkedin_job_id)`, so the receipt resolves identity to exactly one job. Fixing the over-fetch requires the producer library (not in this ticket's allowed paths); `test_receipt_classes_distinguish_parsed_cards_from_deduped_jobs` documents the current behavior and the receipt guarantee.

### Documentation obligations for completion (before Done)

- `docs/reverse-engineering/05-subsystems/acquisition-and-collectors.md` §3.2 (LinkedIn runner options) and §5.1 should record the new dry-run receipt contract and `--dry-run` fixture-backed behavior.
- `docs/subsystems.yaml` acquisition entry: no owned-path/entry-point/invariant change (dry-run contract only) — verify at completion.
- `docs/tickets/completions/T29.md` must be written before Done (not required for In Review).

### Chat/session link

- opencode session transcript for this attempt (chat link included in the Linear attachment and issue comment).
