# T40 attempt log — RUN-39 [T40] Improve employer career URL discovery and source targeting

## Attempt 01 — resumed controller attempt, passed

- **Date:** 2026-09-19
- **Issue:** RUN-39 (`https://linear.app/runr/issue/RUN-39/t40-improve-employer-career-url-discovery-and-source-targeting`)
- **Baseline SHA:** `405c70e7dbc3bc4853d39a46b88358e5d23c4f45` (verified present and ancestor of main)
- **Branch:** `runr-auto/run-39` (existing controller branch — resumed; the ticket-contract branch `feat/t40-career-url-discovery` / worktree `../runr-t40-career-url-discovery` was **not** created because a prior controller attempt already existed and the implementation skill forbids a second branch/worktree for an unknown existing attempt)
- **Worktree:** `C:\Users\ahmed\AppData\Local\RunrAutomation\worktrees\run-39` (clean checkout, `.venv` junction → shared checkout `C:\Users\ahmed\Projects_Local\job-automation\Linkedin Jobs Scrapper\.venv`, Python 3.12.7 verified)
- **Chat link:** this opencode session (no external chat URL available)
- **Implementation commits:**
  - `172745d9` `checkpoint(RUN-39): validated implementation` (prior controller checkpoint, resumed and validated — backend/connectors/ats_router.py, backend/connectors/company_career_discovery.py)
  - `8110eb5e` `feat(RUN-39): add career URL fixtures, receipts, and coverage benchmark` (this attempt: fixtures, JSON-LD scoring, cached receipts, coverage benchmark)
- **Attempt-log metadata commit:** see the commit containing this file.
- **Result:** passed

### Changed files (this attempt, on top of the checkpoint)

- `backend/connectors/company_career_discovery.py` — json_ld confidence branch; `build_discovery_receipt`; `collect_discovery_receipts` (caller-owned cache); `build_career_coverage_benchmark`
- `tests/test_ats_router.py` — Softgarden classification + report-only routing; host-policy mismatch `invalid_target`/`host_policy_mismatch`; unsupported reason-code observability
- `tests/test_company_career_discovery.py` — per-ATS discovery fixtures (Greenhouse, Lever, Workday, Personio, Recruitee, SmartRecruiters, Softgarden); generic JSON-LD fixture; unsupported-external-host fixture; never-returns-homepage guard; cached-receipt test; offline coverage benchmark test with bounded request count

### Commands and results

- `.venv\Scripts\python.exe -m pytest -q tests/test_company_career_discovery.py tests/test_career_url_discovery_security.py tests/test_ats_router.py tests/test_ats_expansions.py` → **54 passed, 21 subtests passed** (ticket safe verification)
- Regression sweep outside the manifest list: `tests/test_career_memory.py tests/test_career_profiles.py tests/test_career_profile_evidence.py` → 44 passed
- Broader employer-producer sweep (`test_employer_*`, `test_master_employer_jobs_catalog.py`, `test_producer_adapters.py`, `test_rc011_employer_outcomes.py`, `test_rc026_benchmark.py`) → 82 passed, **1 failed**: `test_rc011_employer_outcomes.py::test_resume_rechecks_legacy_no_jobs_without_coverage_evidence`. **Pre-existing failure**: reproduced identically at the baseline SHA `405c70e7` with the T40 changes stashed; it exercises `scripts/master_employer_jobs_catalog.py`, which is outside this ticket's allowed paths — not fixed here.

### Acceptance criteria results

- Validated career URL or explicit reason code; never silently uses the homepage → **pass** (`reason_code` closed values, `validated_career_url`, homepage rejected as candidate; covered by tests)
- Fixtures cover Greenhouse, Lever, Workday, Personio, Recruitee, SmartRecruiters, Softgarden, generic JSON-LD, unsupported hosts → **pass** (`CareerDiscoveryFixtureTests`, `AtsRouterTests` softgarden/mismatch tests)
- Provenance, confidence, freshness, host policy observable → **pass** (`provenance`/`freshness`/`host_policy`/`confidence_score` on result + receipt, asserted in tests)
- Employer benchmark shows improved career-targeted coverage without unsafe requests → **pass** (`build_career_coverage_benchmark` offline metric; benchmark test asserts bounded, all-https fetch calls; no live calls anywhere)

### Blockers / deviations

- Worktree contract deviation documented above (resumed existing controller attempt per implementation-skill safety rule).
- Pre-existing unrelated test failure outside the manifest (recorded, not fixed).
- No package installs, no live network, no credentials, no deployment-config changes.
