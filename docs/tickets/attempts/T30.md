# RUN-29 [T30] implementation attempt 01

## Packet

- **Issue:** [RUN-29 — T30 Reconcile release provenance, migration heads, and deployed revisions](https://linear.app/runr/issue/RUN-29/t30-reconcile-release-provenance-migration-heads-and-deployed)
- **Baseline SHA:** `abd670407a8389a57d95d67bc909baa67e6237b9` on `predeployment/render-turso-r2`.
- **Branch:** `feat/t30-release-migration-reconciliation`.
- **Worktree:** `C:\Users\ahmed\Projects_Local\job-automation\runr-t30-release-migration-reconciliation`.
- **Interpreter:** worktree `.venv` junction to the shared project environment; verified `Python 3.12.7`.
- **Research:** `docs/tickets/research/T30.md` was completed before implementation in the shared checkout; Linear handoff moved RUN-29 to grouped `Ready`.
- **Chat/session link:** not exposed by this API context; the Linear issue URL above is the durable work-item link.

## Implementation

- **Implementation SHA:** `540c9b3a6d68c290a46ef9b3b915ee36a444d0f6` (`feat(t30): reconcile release provenance and migration head`).
- **Changed files:**
  - `RELEASE_LEDGER.md` — append-only reconciliation of repository, Render, VPS, historical revisions, and migration head.
  - `docs/RUNR_PRODUCTION_COMPLETION_HANDOFF.md` — documentary/live-verification boundary and T31 checkpoint handoff.
  - `render.yaml` — API and worker head changed from `058_customer_task_queue` to `060_publication_latest_observation_index`.
  - `deploy/start.sh` — fail-closed provenance validation before role startup and code-derived head export.
  - `backend/database/connection.py` — shared release branch/commit and migration-head validator at the database boundary.
  - `backend/repositories/sqlite_migrations.py` — code-derived `current_migration_head()` accessor; no existing migration changed or reordered.
  - `tests/test_rc022_build_release_contract.py` — Render/startup/revision/head contract tests.
  - `tests/test_database_migrations.py` — append-only registry-head/checksum and database-boundary mismatch tests.

## Checks and results

- `.venv\Scripts\python.exe --version` → `Python 3.12.7`.
- Baseline focused suite → `12 passed`.
- TDD red phase → expected failures for the missing head/provenance behavior.
- `.venv\Scripts\python.exe -m pytest -q tests/test_rc022_build_release_contract.py tests/test_database_migrations.py` → **19 passed**.
- Ruff on changed Python files → **All checks passed**.
- Documentation link check from `docs/INDEX.md` → passed with no broken links.
- `git diff --check` → passed.
- Full `.venv\Scripts\python.exe -m pytest -q` → `1935 passed, 98 subtests passed, 13 failed, 6 warnings`; the 13 failures are unrelated backend API/route, Runr automation worktree/provider, and Windows encoding tests.
- The same 13-test failure subset on clean baseline `abd670407a...` → `13 failed, 1 warning`, with the same failure signatures. These are baseline/environment failures, not T30 regressions.

## Acceptance criteria

| Criterion | Result |
|---|---|
| Ledger maps repository, Render, VPS revisions, and migration head | Met in the append-only T30 sections of `RELEASE_LEDGER.md` and `docs/RUNR_PRODUCTION_COMPLETION_HANDOFF.md`; live deployment remains explicitly unverified. |
| Schema gaps become append-only migration work with checksum-safe tests | Met for T30’s boundary: registry head is derived, ordered, unique, and checksum-backed; no competing checkpoint migration was added; T31 owns the checkpoint schema decision. |
| Verification fails on incompatible revisions or heads | Met: known branch/commit mismatches and configured migration-head mismatches raise `DatabaseConfigurationError` before database use/startup. |

## Handoff

Implementation is validated for the T30 scope and should stop at Linear `In Review`.
Do not merge, deploy, run production migrations, or mark the issue `Done`.

## Review follow-up: implementation attempt 02

- **Review finding:** The direct `backend.deployment.release_contract` entrypoint still defaulted to the historical `058_customer_task_queue` head, despite the registry and startup path using `060_publication_latest_observation_index`.
- **Scope authorization:** RUN-29's exact allowed paths were updated to include `backend/deployment/release_contract.py` before this fix.
- **Fix commit:** `dc338709` (`fix(t30): derive release metadata migration head`).
- **Changed files:** `backend/deployment/release_contract.py` now derives the metadata default from `current_migration_head()`; `tests/test_rc022_build_release_contract.py` covers the direct metadata default.
- **Review-fix checks:** direct metadata output reports `060_publication_latest_observation_index`; focused suite is **20 passed**; Ruff passed; `git diff --check` passed.
- **Final handoff:** The implementation is ready for a fresh review. Do not merge, deploy, run production migrations, or mark the issue `Done`.
