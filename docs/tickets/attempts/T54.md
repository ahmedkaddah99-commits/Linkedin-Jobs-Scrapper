# T54 attempt log

## Attempt 01 — 2026-09-21 — implementation complete; all in-scope required checks passed; handoff blocked by a pre-existing out-of-scope e2e regression → `Missing Requirement`

- **Issue:** RUN-54 [T54] — Make the authenticated Jobs first-content path fast and measurable (frontend WS-8 primary; co-owners application-services, http-api-cli, data-domain-model, backend-test-suite, deployment-release-ci).
- **Starting label:** `Ready` (verified: labels `frontend`, `Ready`, `Improvement`; exactly one `Issue Status` child). Assigned `In Progress` after isolation succeeded; re-read verified.
- **Baseline SHA:** ticket baseline `30c8d5f1641951148de3e30f44165e8e69fb764e` (deployment/render-turso-r2). Worktree base = verified permanent predeployment branch `predeployment/render-turso-r2` @ `abd670407a8389a57d95d67bc909baa67e6237b9` (local == origin at attempt start).
- **Branch:** `feat/t54-jobs-loading-performance` (ticket worktree contract; Linear native branch name not used).
- **Worktree (absolute):** `C:\Users\ahmed\Projects_Local\job-automation\runr-t54-jobs-loading-performance` (outside the shared checkout; `.venv` is a directory junction to the canonical shared-checkout venv; junction interpreter verified `Python 3.12.7`). Shared checkout left byte-for-byte unchanged (only reads).
- **Scope manifest:** hashed SHA-256 `786b26d37e1ec7a432341c29ef3bf0c43e8382a2aedc62ec4b9ee498a180d1c4` (built with `runr_automation.scope_router.build_scope_manifest` from `docs/subsystems.yaml` + the ticket's Allowed paths/reading/tests; content hashes over all allowed read files). Registry-required co-owner note: the registry owns `scripts/benchmark_personalized_jobs.py` under `acquisition` and the docs/evidence paths under `docs-and-repository-artifacts`; both were added to the manifest co-owner set solely because `build_scope_manifest` requires declared owners for ticket-granted allowed paths. All final changed paths were re-validated against the manifest `allowed_writes` — all within scope. No controller wave existed for this issue; per-issue conflict check done instead: active tickets RUN-29 (T30, deployment-release-ci) and RUN-35 (T36, backend-test-suite) change none of the Jobs route/service/store/frontend paths (verified by `git diff --name-only` on their branches).
- **Implementation SHA:** `d19c11ba828e70ead1d40575effd29753b8945fd` (`feat(jobs): measure and bound the authenticated Jobs first-content path`), followed by this separate attempt-log metadata commit.
- **Changed files (13):**
  - `frontend/src/lib/api.js` — `markJobsPhase`: privacy-safe `runr-jobs:<phase>` Performance marks labeled `{route:"/jobs", phase, deviceClass, mode, revision}` (no user data/query text/payload content); phase names validated.
  - `frontend/src/context/SessionContext.jsx` — cold `session-connected` mark with bootstrap duration (first successful `/auth/me` or manual connect per page load).
  - `frontend/src/App.jsx` — `route-chunk` mark when the Jobs lazy chunk resolves (cold first, warm after); idle preload of the other Jobs chunks preserved.
  - `frontend/src/components/personalized/JobsWorkspace.jsx` — initial feed request fires immediately (debounce kept only for later filter edits); `AbortController` cancellation + bounded 20 s timeout on feed/detail/company/load-more reads; refresh no longer clears an already loaded feed (stale cards stay visible under a new `refreshing` catalog banner; failed refresh keeps the last verified page with a Retry control); selected-job detail request deduplicated per job id with aborted-fetch retry; marks `route-mounted`, `feed-request-start`, `feed-request-end` (`durationMs` + server `timings.total_ms`), `useful-render` (cards/empty/error), `interactive`.
  - `backend/application/personalized_jobs_service.py` — feed response (card and full views) now carries a privacy-safe `timings` object (`store_query_ms`, `capabilities_ms`, `total_ms`, 0.1 ms rounding) measured around the store query and capabilities projection.
  - `backend/repositories/sqlite_personalized_jobs.py` — bounded, publication-keyed cache for `get_published_filter_capabilities` (a publication's catalog is immutable → pure function of `publication_id`; 8-entry bound, oldest evicted; new head recomputes). Removes one full-catalog `MAX(CASE WHEN …)` aggregate scan per warm feed request; returned values unchanged.
  - `frontend/e2e/phase-d-jobs-cutover.spec.ts` — useful-readiness measurement: 5 reloads of `/jobs`, waits for `runr-jobs:useful-render`, records p50/p75/p95 with `{phase, state, deviceClass, mode, revision, usefulMs, feedRequestMs, documentLoadMs}` per run, attaches `jobs-useful-readiness.json`, asserts p95 < 5000 ms; runs on both desktop-chromium and mobile-chromium projects. Document `load` timing retained separately.
  - `frontend/src/lib/api.test.js` — tests for `markJobsPhase` labels and unsafe-phase rejection.
  - `tests/test_jobs_feed_performance.py` (new) — truthful numeric server timings in both feed views; capabilities cache hit/miss, recompute on new publication, ≤8-entry bound, cache==fresh-recomputation equivalence.
  - `scripts/benchmark_personalized_jobs.py` — p75 added to the JSON report (bounded timing coverage).
  - `docs/phase-c-feed-performance-security.md` — new "T54 feed first-content contract" section.
  - `docs/reverse-engineering/01-architecture/frontend-app.md` — §5.4 T54 readiness contract; §3.3 correction of the `retireLegacyJobsNavigation` claim; §7 e2e row updated.
  - `docs/reverse-engineering/05-subsystems/personalized-jobs-and-customer-app-services.md` — feed timings + capabilities-cache documentation in the data-flow and invariants sections.
- **Checks / results (all via the worktree junction interpreter, Python 3.12.7):**
  - `.venv\Scripts\python.exe --version` → `Python 3.12.7`.
  - Baseline benchmark (pristine `abd67040`, temporary read-only worktree, removed after): feed p50 248.56 ms / p95 276.59 ms; company p50 58.12 / p95 63.13. An earlier single run read 211.97/272.75 — re-measured under identical conditions to rule out machine-state variance.
  - Post-change benchmark: feed p50 439.8 / p75 448.36 / p95 467.33; company p50 105.18 / p95 135.5 — and pristine baseline re-run in the same restored environment: feed p50 437.18 / p95 479.73, company p50 105.75 / p95 123.64. Post-change p95 is equal/better within noise; both far under the warm feed p95 <2 s guardrail.
  - `.venv\Scripts\python.exe -m pytest -q tests/test_phase_c_personalized_jobs.py tests/test_phase_c_feed_performance_security.py tests/test_personalized_jobs_contracts.py tests/test_jobs_feed_performance.py` → **20 passed** (baseline had 17; 3 new focused tests).
  - `npm --prefix frontend run test` → **159 passed, 0 failed** (2 new marks tests; 148 pre-existing before them).
  - `npm --prefix frontend run check` (test + `eslint src --max-warnings=0` + `vite build`) → **passed**.
  - Docs link check (`docs/INDEX.md` Verification) → **passed** (no broken links).
  - Playwright e2e `e2e/phase-d-jobs-cutover.spec.ts` → **FAILED, pre-existing at the base revision, outside T54's Allowed paths.** Both projects fail at the unchanged assertion `getByRole('link', { name: 'Workspaces' })` count 0: `AppShell.jsx` stopped honoring `retireLegacyJobsNavigation` in commit `4dcdda39` ("Adopt new Runr job search design"), so the legacy Workspaces/Runs links always render; the flag is computed in `personalizedJobsConfig.js` but imported by no component. `frontend/src/components/AppShell.jsx` is not in the ticket's Allowed paths, so this was not fixed here. Evidence: built dist with the full e2e env (`VITE_E2E_AUTH/E2E_ADMIN/…=1`) still renders the legacy nav; the bundle contains the unfiltered `Wi=[{label:"Workspaces",…}]` nav array; `git log -S retireLegacyJobsNavigation -- AppShell.jsx` shows removal in `4dcdda39`. A standalone temporary spec (same mocks, not committed; temp file removed after the run) exercised the new readiness instrumentation end-to-end: **desktop useful readiness p50 338 / p75 504 / p95 785 ms; mobile p50 298 / p75 325 / p95 721 ms** (`state:"cards"`, correct `deviceClass`, `feedRequestMs` recorded; all far under the 5 s budget). The committed spec's readiness loop is compiled but its assertions after the failing nav check did not execute.
  - Frontend `node_modules`: the shared checkout's was empty, so the worktree provisioned the locked dependency tree via `npm ci --prefix frontend` (no dependency changes; `package-lock.json` untouched) to run the ticket's own listed verification commands.
- **Incident recorded for transparency:** while removing the temporary baseline-measurement worktree, `git worktree remove --force` recursed through its `.venv` directory junction and emptied the canonical shared-checkout `.venv`. The canonical environment was restored in place (`Python 3.12.7` venv + `pip install -r requirements.txt -r requirements-dev.txt` + `pyyaml`, which `runr_automation` imports); all required checks were then re-run through the restored environment with equivalent results. `.venv` is git-ignored, so no tracked state was affected.
- **Acceptance criteria:**
  - Instrumented timings for session-connected, route chunk, route-mounted, feed request start/end, server phase (`timings.total_ms`), first useful card/empty/error render, route interactive → **met**; each mark is privacy-safe and labeled with route, device class, cold/warm mode, and revision.
  - Repeatable local Playwright test recording p50/p75/p95 for desktop and mobile measuring useful readiness → **met in the committed spec** (`jobs-useful-readiness.json` per project); verified executing via the standalone temporary run (desktop p50 338/p95 785; mobile p50 298/p95 721).
  - Controlled baseline and post-change report → **met** (pristine abd67040 benchmark vs post-change benchmark in the same restored environment; numbers above).
  - Guardrails remain: warm feed p95 <2 s → **met** (local 467.33 ms post vs 479.73 ms pristine baseline); useful content <5 s → **met locally** (p95 ≤ 785 ms); Render-like live verification remains the release/verification dependency, not attempted here.
  - Initial request bounded to 25 cards + cursor → **met** (unchanged `limit=25`); no unbounded prefetch (idle preloads unchanged; detail dedupe added).
  - Truthful loading/empty/stale/failure/retry states; no blanking a usable feed during refresh → **met** (`refreshing` banner; last-verified page retained with retry).
  - Auth, dispositions, filters, scrubbing, verified Apply URL rules, accessibility, Jobs actions → **met** (no changes to those paths; 159 frontend unit tests + 20 backend focused tests pass; e2e actions block unchanged but blocked by the pre-existing nav failure).
  - Bottlenecks outside allowed paths documented as follow-up children → **done**: AppShell `retireLegacyJobsNavigation` wiring regression filed as a follow-up child under RUN-53 (see Linear).
- **Result:** implementation complete and validated within scope; the e2e flow gate cannot pass until the out-of-scope AppShell regression is fixed by its owner. Per the skill's blocked-outcome rule this attempt hands off with `Missing Requirement` (not `In Review`, not `Done`); branch, remote push, and worktree remain intact for review and later merge.
- **Chat link:** opencode CLI session (local, this conversation); Linear attachment carries the same evidence as this log.
