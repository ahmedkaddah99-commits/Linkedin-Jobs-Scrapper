# T39 attempt log

## Attempt 01 — 2026-09-19 — passed (with documented notes)

- **Issue:** RUN-38 [T39] — Add VPS resource and producer health telemetry (WS-07 primary; co-owners WS-03, WS-10).
- **Baseline SHA:** `405c70e7dbc3bc4853d39a46b88358e5d23c4f45` (verified: exists on `main`, is ancestor of shared-checkout HEAD `30c8d5f1`; worktree checked out at this exact SHA).
- **Branch:** `feat/t39-vps-producer-telemetry`.
- **Worktree (absolute):** `C:\Users\ahmed\Projects_Local\job-automation\runr-t39-vps-producer-telemetry` (created outside the shared checkout; `.venv` is a directory junction to the canonical shared-checkout venv; junction interpreter verified `Python 3.12.7`).
- **Implementation SHA:** `94d31b13df83ad908eacd74d0da29c3fe17624c9` (`feat: add bounded producer health telemetry receipts`).
- **Research evidence:** Linear attachment "RUN-38 research evidence — Research State: Current"; repo copy `docs/tickets/research/T39.md` (untracked, research-phase artifact, written before isolation).
- **Changed files:**
  - `scripts/run_manifested_linkedin.py` — bounded `telemetry` block (schema `runr.producer.telemetry.v1`): source, emitted_at, source_version, resource peaks via `getrusage` (POSIX-guarded), reason_code.
  - `scripts/run_manifested_employer.py` — same.
  - `scripts/publish_producer_states.py` — publisher telemetry: per-source `checkpoint_age_seconds`, `stale_checkpoint`, `publish_lag_seconds`, `last_cycle_id`, `last_publication_id`; staleness threshold env `RUNR_TELEMETRY_STALE_CHECKPOINT_SECONDS` (default 86400); telemetry attached to every `run_delivery` return path.
  - `deploy/run-acquisition-source.sh` — `emit_telemetry` writes `receipts/<source>-latest-telemetry.json` with classified `reason_code` (`ok`/`lock_overlap`/`stall_timeout`/`validation_failed`/`failed`); emitted on lock overlap and before the receipt writer so it survives failure.
  - `deploy/run-acquisition-publisher.sh` — same (publisher lock overlap on all three lock paths).
  - `deploy/systemd/runr-acquisition-publisher.service` — `RUNR_TELEMETRY_STALE_CHECKPOINT_SECONDS=86400`. No timer ownership change; no `SuccessExitStatus` change (T27 scope, WS7-G7).
  - `tests/test_rc023_producer_state_paths.py` — stale assertion fixed (`table_count == 16`; `collection_cursor` table was added to the baseline's StateStore by `819d33e4` without updating this count — the test failed at the pristine baseline before any T39 change); new tests: bounded telemetry from both manifested wrappers (dry-run), publisher lag/staleness + env threshold, WSL harness proofs that wrapper receipts/telemetry survive validation failure and report lock overlap (exit 75), and `sh -n` syntax checks of both wrappers.
  - `docs/reverse-engineering/02-deployment/vps-runtime-and-acquisition-timers.md` — new "5a. Producer health telemetry" section (retention/redaction/threshold documentation; template-mandated subsystem-doc completion obligation).
- **Checks / results:**
  - `.venv\Scripts\python.exe -m pytest -q tests/test_rc023_producer_state_paths.py tests/test_producer_state_delivery.py` → **16 passed** (required safe verification).
  - `.venv\Scripts\python.exe -m pytest -q tests/test_rc022_build_release_contract.py tests/test_rc023_vps_runtime.py tests/test_rc024_backup_restore.py tests/test_acquisition_runtime_manifest.py` → 25 passed, 1 failed: `test_rc023_vps_runtime.py::test_logs_are_bounded_and_application_target_includes_acquisition_worker`. **Pre-existing at the baseline** (fails with all T39 changes stashed; documented as WS7-G1/C6 stale acquisition-worker assertion in the same doc). Out of T39 scope; T27 owns that reconciliation.
  - `python -m ruff check` on all changed Python files → All checks passed.
  - Documentation link check (`docs/INDEX.md` Verification §1) → passed (no broken links).
  - WSL fixture proofs (in-suite): wrapper receipts + telemetry survive failure (`/bin/false` python → `validation_failed`, metrics file preserved), lock overlap → `reason_code: lock_overlap`, `exit_code: 75`.
- **Acceptance criteria:**
  - Bounded receipt with revision/times/counts/reasons/peaks/exit status → met (receipt v1 + telemetry v1; revision from `RUNR_SOURCE_VERSION`/`RUNR_RELEASE_COMMIT`).
  - Identifies stalls, overlap, stale checkpoints, publish lag without secrets → met (`stall_timeout`, `lock_overlap`, `stale_checkpoint`, `publish_lag_seconds`; allowlisted keys only; tested).
  - Retention/redaction documented and tested → met (doc section 5a; redaction key assertions in tests).
  - Fixture and VPS dry-run prove receipts survive failure → fixture proof in-suite (WSL harness + wrapper dry-run telemetry tests). True VPS-host dry-run requires operator access (same external limitation recorded on RUN-25); the documented command is `sh -x deploy/run-acquisition-source.sh linkedin` under systemd with `--dry-run` runner overrides, or `scripts/run_manifested_linkedin.py --dry-run` — recorded for the operator; NOT executed here (no host contacted).
- **Notes:**
  - Pre-existing empty reservation `runr-auto/run-38` (branch + worktree `C:\Users\ahmed\AppData\Local\RunrAutomation\worktrees\run-38`, 0 commits beyond baseline, clean) was found and left untouched; no second attempt existed, so this ticket's contract branch was created fresh at the baseline.
  - No package installs, no live network, no production access, no secrets/PII, no payload capture, no timer ownership change.
