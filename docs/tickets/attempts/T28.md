# T28 — Attempt log

## Attempt 02 — RUN-27 integration fix (2026-09-19)

- **Issue:** RUN-27 [T28] (integration fix for attempt 01 open item O-1)
- **Session:** opencode CLI interactive session (continuation)
- **Baseline:** attempt 01 implementation commit `a23fb3fa4495f68ef3c047fc0dc49787242e4d4c`
- **Branch / worktree:** `feat/t28-employer-producer-reliability` / `C:\Users\ahmed\Projects_Local\job-automation\runr-t28-employer-producer-reliability`
- **Implementation commit SHA:** `d28f6c1240dee919c22d65747196d81aff4442f5`
- **Result:** PASS — closed O-1.

### Change (single file: `scripts/master_employer_jobs_catalog.py`)

`EmployerState.due_company_keys` now additionally calls new helper `_recheck_required_negative_keys(scheduled_keys)`: among scan-scheduled keys, persisted `no_jobs` rows are added to the due set unless their `coverage_receipts.classification` is `confirmed_complete`. State databases without a receipts table (`sqlite3.OperationalError`) have no evidence, so their no_jobs rows are rechecked. Confirmed-complete rows keep their normal schedule; nothing else in the collector changed.

### Commands and results

| Command | Result |
|---|---|
| `.venv\Scripts\python.exe --version` | Python 3.12.7 |
| `pytest -q tests/test_rc011_employer_outcomes.py::test_resume_rechecks_legacy_no_jobs_without_coverage_evidence` | 1 passed (test unmodified) |
| `pytest -q tests/test_rc012_employer_concurrency.py tests/test_rc011_employer_outcomes.py tests/test_employer_bounded_cycles.py` | **27 passed, 0 failed** |
| `pytest -q tests/test_master_employer_jobs_catalog.py tests/test_employer_coverage_receipts.py tests/test_employer_coverage_evidence.py` | 65 passed, 0 failed |
| Sweep: rc023, site_fallbacks, traversal, source_eligibility_manifest, bounded_cycles | 38 passed, 1 failed (pre-existing baseline LinkedIn rc023 failure, unrelated) |

### Requirements preserved

Browser reuse (`ReusableBrowser`), retry limits, five-minute dry-run receipts, publication completeness, and source attribution are unchanged; the failing test was not modified; `a23fb3fa` was not amended (separate commit `d28f6c12`).

## Attempt 01 — RUN-27 (2026-09-19)

- **Issue:** RUN-27 [T28] Repair employer-site producer scheduling and browser reuse
- **Session:** opencode CLI interactive session (chat link not available in this environment; research evidence attached as `RUN-27-RESEARCH` on the issue)
- **Baseline SHA:** `405c70e7dbc3bc4853d39a46b88358e5d23c4f45`
- **Branch:** `feat/t28-employer-producer-reliability`
- **Worktree (absolute):** `C:\Users\ahmed\Projects_Local\job-automation\runr-t28-employer-producer-reliability`
- **Implementation commit SHA:** `a23fb3fa4495f68ef3c047fc0dc49787242e4d4c`
- **Attempt-log commit SHA:** recorded in the Linear attachment after this commit
- **Result:** Implemented; required suite green except one pre-existing, out-of-scope failure (see below).

### Changed files (implementation commit)

- `backend/connectors/employer_site_fallbacks.py` — extracted the per-page navigation into `_collect_from_page` / `_finalize_snapshot`; added `ReusableBrowser` (one bounded Chromium process, internal lock serializes navigations, lazy launch, teardown on dead browser, `close()`); `fetch_browser_snapshot` now delegates to a session (throwaway when none supplied → legacy behavior identical) with optional `retry_attempts` (bounded ≤ 3, retries only when a timeout produced zero jobs).
- `scripts/run_manifested_employer.py` — added opt-in `--dry-run-receipt` (with `--dry-run`) producing `employer_dry_run_receipt.json` aggregating job outcomes (discovered / accepted / rejected / deduplicated / failed) from staged manifest cohort + durable state without mutation, wall-clock budgeted by `--dry-run-minutes` (default 5). Baseline `--dry-run` forwarding to `run_collection` is preserved (`tests/test_rc023_producer_state_paths.py` pins it).
- `scripts/verify_employer_browser_runtime.py` — loopback smoke now runs two fetches through one `ReusableBrowser` and asserts a single browser launch.
- `tests/test_rc011_employer_outcomes.py` — added: reuse + retry-within-budget, timeout partial-failure preservation (retry skipped when jobs already observed), dry-run receipt classification, missing-state zeros, wrapper `--dry-run-receipt` flag test.
- `tests/test_rc012_employer_concurrency.py` — added: one process across many fetches; concurrent fetches serialize to one navigation and one launch.

Not edited (no change required): `backend/connectors/company_career_sites.py`, `backend/connectors/company_career_discovery.py`, `backend/acquisition/employer_coverage.py`, `tests/test_employer_bounded_cycles.py`.

### Commands and results (`.venv\Scripts\python.exe`, Python 3.12.7 junction to canonical env)

| Command | Result |
|---|---|
| `python -m pytest -q tests/test_rc012_employer_concurrency.py tests/test_rc011_employer_outcomes.py tests/test_employer_bounded_cycles.py` | 26 passed, 1 failed |
| `python scripts/run_manifested_employer.py --help` | exit 0 |
| Regression sweep: rc023, rc012, rc011, bounded_cycles, site_fallbacks, master_employer_jobs_catalog, traversal, coverage_receipts, coverage_evidence, source_eligibility_manifest | 126 passed, 2 failed (both pre-existing) |

### Acceptance criteria

| Criterion | Result |
|---|---|
| Tests cover browser reuse, timeout, retry, host concurrency, partial failure | PASS (new tests in rc011/rc012) |
| Five-minute dry-run receipt reports discovered, accepted, rejected, deduplicated, failed jobs | PASS (`--dry-run --dry-run-receipt`, budgeted 300 s default) |
| No change weakens publication completeness or source attribution | PASS (no publication/mapping path touched; `_is_accepted_job_page` gate reused for accepted count) |

### Failure / blocker reason

- `tests/test_rc011_employer_outcomes.py::test_resume_rechecks_legacy_no_jobs_without_coverage_evidence` fails **at the baseline SHA** too: a legacy `no_jobs` row without coverage evidence is scheduled 24 h ahead by `EmployerState.save` → `record_company_checkpoint`, so `due_company_keys` excludes it on resume. The repair lives in `scripts/master_employer_jobs_catalog.py`, which is outside this ticket's exact allowed paths; weakening the pinned test is prohibited. Recorded as open item O-1 (owner decision / successor ticket).
- `tests/test_rc023_producer_state_paths.py::test_linkedin_explicit_state_keeps_generation_journal_and_db_outside_exports` fails at the baseline (LinkedIn-side, unrelated to this ticket's paths).

### Prohibitions honored

No live provider calls in tests (scripted Playwright doubles only); no credential logging; no unbounded browser/process creation (one launch per session/fetch, launch_count asserted in tests); shared checkout left untouched; no packages installed.
