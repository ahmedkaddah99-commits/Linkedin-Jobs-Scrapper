# F54 attempt log

## Attempt 01 — 2026-09-21 — passed

- **Issue:** RUN-55 [F54] — Restore the retired legacy-nav switch in AppShell so the Jobs e2e cutover spec can pass (WS-8 frontend primary).
- **Baseline SHA:** `abd670407a8389a57d95d67bc909baa67e6237b9` (verified: exact HEAD of the permanent predeployment branch `predeployment/render-turso-r2`; worktree created at this exact SHA).
- **Branch:** `ahmedkaddah99/run-55-f54-restore-the-retired-legacy-nav-switch-in-appshell-so-the` (Linear native branch name).
- **Worktree (absolute):** `C:\Users\ahmed\AppData\Local\RunrAutomation\worktrees\run-55` (outside the shared checkout; shared checkout left byte-for-byte unchanged; `.venv` is a directory junction to the canonical shared-checkout venv; junction interpreter verified `Python 3.12.7`).
- **Research evidence:** `docs/tickets/research/F54.md` (Research State: Current; research artifact written before isolation, untracked in the shared checkout per existing convention).
- **Implementation SHA:** `2573d851` (`fix: restore the retired legacy-nav switch in AppShell for the Jobs e2e cutover`).
- **Changed files (all within Allowed paths):**
  - `frontend/src/components/AppShell.jsx` — restored the pre-`4dcdda39` contract: `retireLegacyJobsNavigation` is imported again from `../lib/personalizedJobsConfig` and `userNavItems` filters the legacy `Workspaces` and `Runs` entries out when the flag is on with real Jobs data mode. The `4dcdda39` admin-branch removal was not re-added; the current AppShell renders `userNavItems` unconditionally, so the filtered list applies to every authenticated user.
  - `frontend/e2e/phase-d-jobs-cutover.spec.ts` — two truthfulness fixes to the test double / stale copy, no assertion weakened:
    - The mocked `/v1/personalized-jobs/job-a*` backend is now stateful (`hide`/`restore`/`save`/`applied` update a `user_state` the detail route reflects). Previously the detail mock always returned `user_state: "none"`, so the component's truthful refetch (detail effect depends on `rawJobs`; `JobsWorkspace.jsx:434`) reverted the optimistic hide/restore state before the spec could click `Restore job`. In production the refetch agrees with the optimistic update; the mock now models the real backend. All assertions unchanged.
    - `Full job posting` → `Employer job description`: commit `4dcdda39` renamed the right-panel segmented-control tab; the spec still referenced the retired label. The click target and the subsequent `Original Posting` visibility assertion are unchanged in intent.
  - `docs/reverse-engineering/01-architecture/frontend-app.md` — §3.3 "Navigation" paragraph rewritten to describe the restored behavior (personalized nav + Jobs entry; Workspaces/Runs filtered when `VITE_REPLACE_LEGACY_JOBS_NAV` + real mode is on for every authenticated user; stale pre-`4dcdda39` "reduced nav Home/Jobs/Job tracker/Documents/Refer" description removed).
- **Environment notes:**
  - The canonical shared-checkout `.venv` was found empty at session start (no `Python 3.12.7` interpreter); the owner rebuilt it in-session and verification confirmed `Python 3.12.7` before any ticket command. The worktree junction exposes the rebuilt canonical venv. No Python command is required by this ticket's checks.
  - `npm ci` was run in the worktree `frontend/` to materialize the locked dependencies (the fresh worktree had no `node_modules`; the first `npm run check` failed with `ERR_MODULE_NOT_FOUND: react`). No dependency manifest was changed.
  - The literal command `npm --prefix frontend exec playwright test ...` run from the worktree root resolves Playwright without the frontend config (no baseURL/webServer/projects); it was run with `frontend/` as the working directory, which is the same command semantics with the config active. Recorded so the command contract can be clarified later.
- **Checks / results:**
  - `npm --prefix frontend run check` (node --test 147 tests + ESLint + `vite build`) → **all passed** (run after the AppShell/doc changes; the spec and doc are outside this command's scope).
  - `npm exec playwright test e2e/phase-d-jobs-cutover.spec.ts` (workdir `frontend/`, e2e production build via `serve-production-e2e.mjs` with `VITE_E2E_AUTH=1`, `VITE_E2E_ADMIN=1`, `VITE_PERSONALIZED_JOBS_EXPERIENCE=1`, `VITE_PERSONALIZED_JOBS_DATA_MODE=real`, `VITE_REPLACE_LEGACY_JOBS_NAV=1`) → **2 passed** (`desktop-chromium`, `mobile-chromium`) in 20.7s. The previously failing `Workspaces`/`Runs` nav assertions pass (page snapshot shows neither link); `jobs-production-performance` DOMContentLoaded/load ≈ 146 ms logged.
  - Worktree `git status` after checks → only the three allowed files modified; e2e byproducts (`screenshots/*.png` regeneration, `test-results/`) restored/ignored, not committed.
- **Acceptance criteria:**
  - `VITE_REPLACE_LEGACY_JOBS_NAV=1` (real mode) hides the legacy Workspaces/Runs nav entries in AppShell → **met** (restored filter; verified by the passing cutover spec's `toHaveCount(0)` assertions and page snapshot).
  - `npm --prefix frontend exec playwright test e2e/phase-d-jobs-cutover.spec.ts` passes on desktop-chromium and mobile-chromium against the e2e production build → **met** (2 passed; run with `frontend/` as cwd so the config applies, see environment notes).
  - `docs/reverse-engineering/01-architecture/frontend-app.md` §3.3 navigation paragraph matches the restored behavior → **met**.
- **Prohibited changes:** none. No e2e expectation faked or accessibility weakened (the two spec edits fix a lying mock and a stale label; assertions untouched). No deploy config touched. No packages added. No production access, no credentials, no secrets.
- **Handoff:** issue left `In Review` with this attempt attached; branch and worktree intact for predeployment integration (the RUN-54 e2e gate this ticket blocks can re-run against this revision after merge).
