# T44 (RUN-44) attempt log

## Attempt 01 — 2026-09-19 — PASSED (with one pre-existing baseline failure recorded)

- **Issue:** [RUN-44](https://linear.app/runr/issue/RUN-44/t44p0-recovery-preserve-every-recovered-acquisition-asset-off-host) 🚨 [T44][P0 RECOVERY] Preserve every recovered acquisition asset off-host
- **Session:** opencode CLI session in `C:\Users\ahmed\Projects_Local\job-automation\Linkedin Jobs Scrapper` (chat transcript in the issuing CLI; no external chat link)
- **Baseline SHA:** `30c8d5f1641951148de3e30f44165e8e69fb764e` (branch `deployment/render-turso-r2` source; verified in shared checkout before isolation)
- **Branch:** `feat/t44-p0-acquisition-offhost-preservation`
- **Worktree:** `C:\Users\ahmed\Projects_Local\job-automation\runr-t44-p0-acquisition-offhost-preservation` (created this attempt; `.venv` junction → shared-checkout canonical venv, Python 3.12.7)
- **Implementation SHA:** `9fa3fc62386007a6d75c5accc78c8936a769f9e2`
- **Attempt-log metadata SHA:** recorded after this log's own commit
- **Result:** PASSED — implementation and executed off-host preservation both succeeded

### Exact changed files (implementation commit)

- `scripts/acquisition_state_backup.py` — added `preserve-recovery-set` / `verify-recovery-set` / `scheduled-backup` layers (versioned manifest loading with drift fail-closed, immutable per-asset uploads with sha256 metadata, HEAD/download verification, SQLite integrity/schema/row-count checks, `git bundle verify`, receipt-gated scheduled backup with ≥2-generation retention)
- `deploy/systemd/runr-acquisition-backup.service` (new) — hardened oneshot, `runr-acquisition`, linkedin+employer `scheduled-backup --upload`, fail-fast
- `deploy/systemd/runr-acquisition-backup.timer` (new) — `*-*-* 05:00:00`, `Persistent=true`, `RandomizedDelaySec=300`
- `deploy/setup.sh` — installs both units, `systemctl enable --now runr-acquisition-backup.timer`
- `deploy/vps-runtime-contract.json` — `roles.acquisition.backup_unit/backup_timer/backup_entrypoint`; retention keys (local ≥3, remote ≥7, receipt-gated prune, object prefix)
- `deploy/acquisition.env.example` — `RUNR_ACQUISITION_BACKUP_*` variables
- `tests/test_rc024_backup_restore.py` — 6 new tests (preservation, drift refusal, isolated drill, fail-closed missing object, scheduled backup upload-before-prune)
- `tests/test_rc023_vps_runtime.py` — 5 new tests (unit hardening, timer schedule, setup wiring, contract ownership, env example)
- `docs/reverse-engineering/02-deployment/vps-runtime-and-acquisition-timers.md` — units/timers/call-graph/status updated; **WS7-G11 closed** (§6, §9, §10, agent-context candidate 5 marked done)
- `docs/reverse-engineering/03-data/acquisition-source-state.md` — §5.4 recovered-set + off-host preservation record
- `docs/subsystems.yaml` — deployment-release-ci invariant for receipt-gated pruning
- `data/audit/runr_source_state/2026-09-19/` — `offhost_preservation_manifest.json`, `offhost_preservation_runbook.md`, `offhost_preservation_verification.json`, `preservation-receipt.json`, plus the ticket's referenced research evidence (`recovery_manifest.md`, `critical_recovery_ticket_research.md`, copied untracked from the shared checkout)

### Commands and results

| Command | Result |
|---|---|
| `.venv\Scripts\python.exe --version` | Python 3.12.7 |
| `.venv\Scripts\python.exe -m pytest -q tests/test_rc024_backup_restore.py tests/test_rc023_vps_runtime.py` | 26 passed, 1 failed — the failure is `test_logs_are_bounded_and_application_target_includes_acquisition_worker` (`tests/test_rc023_vps_runtime.py:99`), the documented pre-existing WS7-G1 stale-target assertion, failing at baseline before any T44 change (both files it reads, `runr-journald.conf` and `runr.target`, are untouched) |
| `.venv\Scripts\python.exe scripts/acquisition_state_backup.py --help` | OK; new subcommands listed |
| doc-link check (`docs/INDEX.md` Verification §1) | OK, no broken links |
| `git bash -n deploy/setup.sh` (Git-for-Windows bash) | OK |
| `systemd-analyze verify deploy/systemd/runr-acquisition-backup.service …` | NOT RUN — requires a systemd host (Linux); noted for review |
| `preserve-recovery-set` (local validation, no network) | OK: 13 assets validated |
| `preserve-recovery-set --upload` (R2) | OK: 13/13 objects uploaded, 4,915,180,506 bytes, receipt `preservation-receipt.json` (published_at 2026-09-19T20:17:55Z) |
| `verify-recovery-set --download-dir <isolated temp dir>` (isolated restore drill) | OK: HEAD verification ok 13/13; isolated downloads re-hashed ok 13/13; SQLite integrity ok with expected tables/counts (LinkedIn `jobs` 188,206 / `job_company_observations` 188,206; employer `companies` 428 / `jobs` 2,612; enrichment `url_resolution` 15,454); `git bundle verify` ok (complete history, sha1) |
| `verify-recovery-set` (HEAD-only, saved as `offhost_preservation_verification.json`) | OK: 13/13 head ok (verified_at 2026-09-19T20:36:52Z) |

### Acceptance-criteria results

1. **13-file versioned manifest (bytes, SHA-256, logical role, source revision, retention class):** PASS — `data/audit/runr_source_state/2026-09-19/offhost_preservation_manifest.json` (schema `runr.acquisition.recovery-manifest.v1`).
2. **All asset classes off-host with matching remote sizes/hashes:** PASS — 13/13 objects under `runr/acquisition/checkpoints/recovery/2026-09-19/`; HEAD + isolated download verification matches local bytes and SHA-256 (`offhost_preservation_verification.json`).
3. **Isolated remote restore drill (SQLite integrity/schema/counts + Git bundle verify, no active-state touch):** PASS — drill download dir `C:\Users\ahmed\AppData\Local\Temp\runr-restore-drill-t44` (kept for review; active state untouched).
4. **systemd-owned schedule (SQLite-consistent checkpoints, upload before manifest commit, failure reporting, ≥2 verified generations before pruning):** PASS in code + tests — `runr-acquisition-backup.service`/`.timer`; `scheduled-backup` uploads database before manifest; prune is receipt-gated with local keep ≥2 (default 3) / remote keep ≥2 (default 7). Host enablement (`setup.sh` runs `systemctl enable --now`) is committed but host activation itself belongs to VPS host scope (U2).
5. **RPO/RTO, prefix, encryption/access policy, operator runbook documented:** PASS — `data/audit/runr_source_state/2026-09-19/offhost_preservation_runbook.md`.
6. **Evidence preserves the recovered counts:** PASS — manifest + drill re-verified 188,206 LinkedIn jobs; 2,612 employer jobs; 428 employer companies; 15,454 URL resolutions; 12,059 resolved LinkedIn IDs (12,059 is the recorded resolution-status count of the preserved 17,601-row CSV; the drill verifies the file by byte size + SHA-256, the other five by downloaded SQLite row counts).

### Notes / residual scope

- WS7-G1 stale assertion (`test_rc023_vps_runtime.py:99`) left untouched: owned by the C5/C6 ticket candidate; failing at baseline.
- `systemd-analyze verify` deferred to a systemd host (Windows worktree).
- No production restore, no active-state overwrite, no scraping-provider requests, no secrets in manifests/logs/tickets; R2 credentials only via the environment contract file (`user_config/.env`, gitignored, values never printed).
