# RUN-41 / T42 acceptance attempt 11 — 2026-09-25

## Exact revisions and inputs

- Permanent predeployment merge: `56352359c65aafe8c899eb7be2dbbe44fbd391c8` (PR #29; all GitHub checks passed, including Docker).
- VPS archive SHA-256: `cb0cbae50dd51e7ba8fc1dcb15173cf012897553d100e2cc4429dbbde9302481`. The archive and six relevant tracked-file hashes were verified before switching `/opt/runr`. The staged Python version was 3.12.7.
- Source eligibility manifest SHA-256: `72b61f100a0d9edbba315b5f19db589f40cfecd42c19ce3ef95b78b331621873`. Publisher manifest hash: `6bfcba5c01985402d2d1278e8b726baa8e4ac3332e6527be40bc433ab663e447`.
- Verified company registry SHA-256: `31ed996ab2b2d0723b00a6634cdcba02901862f95ffb1b2f3c9d8432bb457446`.
- Isolated VPS state: `/srv/runr/state/t42-attempt11-20260925`; active authoritative pointer remained `/srv/runr/state/versions/t42-restored-20260923`.

## Producer and publisher receipts

| Stage | Bounded result |
| --- | --- |
| Employer systemd | Exit 0; one exact manifest company; four of eight requests; 10 Workday records; 2,622 total persisted employer jobs. CPU 8.671 s and peak RSS 446,812,160 bytes. |
| Verified employer record | `Senior Brand Manager Foods`, source rowid 2613, 4,268-character description and a working dedicated Workday `/apply` URL. Source-level T32 validation returned no rejection reasons after registry enrichment. |
| LinkedIn | Attempt 09 at the prior exact predeployment revision selected two companies, both partial, with 59 requests, 48 valid cards, 48 detail successes, zero malformed cards, zero detail failures, and 48 jobs written. The final public head retains one LinkedIn job. |
| Publisher first chunk | Committed publication `acq_publication_ec6072ea3f094fd5864f0e0554e047cb`; no employer jobs delivered in that chunk. |
| Publisher second chunk | The 300 s control stopped before delivery after 359.235 s; checkpoint did not advance. A bounded retry with a 900 s control completed in 432.078 s, delivered 10 employer observations and committed publication `acq_publication_7131e20f8b9347aab40b27ab1c6ecfea`. |
| Public Turso head | Publication policy v2; three jobs: one LinkedIn, two Workday. Seven incomplete Workday summaries were rejected for missing required detail fields. |

The employer database copied from isolated VPS state to the local publisher matched SHA-256 `75c7d483929041fd326eafe1e567f6f68ba0e1db4c912706e390819f66ace7cb`. Render API and worker both reported the exact merge revision live; API `/health/live` and `/health/ready` returned HTTP 200. The publisher used the existing Render Turso binding in its local process. No Turso credential was copied to the VPS.

## Rollback and remaining acceptance

The VPS maintenance switch restored the original `/opt/runr` tree. Before and after hashes of code, base unit files, and authoritative producer state matched. The temporary bounded service drop-in was removed. All three acquisition timers and the worker were active after rollback, and the active state pointer was unchanged.

An admin-enforced browser policy denied agent access to `app.userunr.com`, so a signed-in jobs-page check could not be performed. Product-owner confirmation was requested. Deployment branch promotion remains pending: its current revision is 109 commits and 216 files behind predeployment, and cherry-picking the first RUN-41 commit alone conflicts in both acquisition entrypoints and the VPS runtime checks. The isolated cherry-pick was aborted with a clean worktree. Do not treat the three-job database head or API health responses as proof that the signed-in UI rendered those jobs.

## Product-owner release decision — 2026-09-25

The product owner approved the requested signed-in jobs visibility confirmation and the temporary publisher timing exception: the successful second chunk took 432.078 seconds against the 300-second target. Track the throughput improvement under RUN-35. The product owner explicitly authorized promotion of the full permanent predeployment branch, including the other completed tickets, to the protected deployment branch. The earlier cherry-pick attempt remains aborted; this release uses a full branch merge. The agent could not directly inspect the signed-in page because of the enforced browser policy; the visibility confirmation is from the product owner.
