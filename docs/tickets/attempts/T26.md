# RUN-25 [T26] Attempt 02

## Issue

- **ID:** RUN-25
- **Title:** [T26] Verify live Render, Turso, and VPS topology and database binding
- **Baseline SHA:** `405c70e7dbc3bc4853d39a46b88358e5d23c4f45`
- **Branch:** `feat/t26-production-topology`
- **Worktree:** `C:\Users\ahmed\Projects_Local\job-automation\runr-t26-production-topology`
- **Implementation commit:** `c1407351`
- **Attempt-log commit:** *(this file)*

## What changed

| File | Change |
|------|--------|
| `tests/test_rc023_vps_runtime.py` | Replaced stale target assertion; added tests for VPS acquisition unit/timer ownership, command lines, hardening, env boundary, Turso intent, and Render release-branch/Turso binding. |
| `tests/test_rc023_producer_state_paths.py` | Fixed stale LinkedIn state table count; added test proving `publish_producer_states` creates `acquisition_publisher_checkpoints` with the expected columns. |

## Safe verification

```powershell
.venv\Scripts\python.exe -m pytest -q tests/test_rc023_vps_runtime.py tests/test_rc023_producer_state_paths.py
```

Result:

```
20 passed in 2.54s
```

## Acceptance criteria results

| Criterion | Result | Evidence |
|-----------|--------|----------|
| Document exact VPS unit/timer ownership and command lines | **Verified statically** | `test_vps_acquisition_units_run_as_dedicated_user_with_hardening`, `test_vps_acquisition_producer_command_lines_and_timer_ownership` assert User/Group, ExecStart, PartOf, WantedBy, timer Unit/OnCalendar for linkedin, employer, and publisher. |
| Prove or falsify that producers/publisher use the intended Turso/libSQL and release SHA | **Intent verified statically; live binding not observed** | `test_vps_acquisition_env_intends_turso_and_source_version_placeholder` asserts `.env.acquisition` sets `DATABASE_BACKEND=turso` and `RUNR_SOURCE_VERSION=replace-with-deployed-git-sha`. `test_render_customer_plane_intends_turso_and_release_branch` asserts all three Render services share `deployment/render-turso-r2` / `runr-contract-v1` and API/worker use `DATABASE_BACKEND=turso`. Actual host env values and deployed SHAs require VPS/Render dashboard access. |
| Capture redacted publication-head, checkpoint-table, and revision evidence | **Static schema verified; live capture unavailable** | `test_publisher_creates_checkpoint_table_with_expected_columns` proves the publisher creates `acquisition_publisher_checkpoints`. Live publication-head count, checkpoint rows, and revision evidence require the operator-host visibility command. |

## External blockers still in force

- **VPS/Render live access** is an external prerequisite; this attempt did not run `systemctl`, `render ssh`, or any Turso query.
- The approved read-only production visibility command on the operator host was not executed.
- No secrets, env edits, restarts, Turso mutations, or production runs were performed.

## Next step

Run the operator-host visibility command to collect redacted unit status, deployed revisions, publication-head count, and checkpoint-table presence, then attach the redacted output to this ticket before moving to `Done`.
