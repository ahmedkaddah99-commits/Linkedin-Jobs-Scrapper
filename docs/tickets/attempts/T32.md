# RUN-31 / T32 implementation attempts

## Attempt 01 — blocked by missing project virtual environment

- Issue: `RUN-31` / `[T32] Define and enforce the minimum publishable job and company contract`
- Baseline SHA: `405c70e7dbc3bc4853d39a46b88358e5d23c4f45`
- Branch: `feat/t32-publication-completeness-contract`
- Worktree: `C:\Users\ahmed\Projects_Local\job-automation\runr-t32-publication-completeness-contract`
- Implementation commit: `92b189f7` (`feat(t32): enforce publishable job and company fields`)
- Metadata commit: this commit
- Chat link: unavailable in the API session; Linear issue: https://linear.app/runr/issue/RUN-31/t32-define-and-enforce-the-minimum-publishable-job-and-company

### Scope changed

- `backend/acquisition/job_publication_completeness.py`
- `tests/test_job_publication_completeness.py`

The validator now models blocking company logo/enrichment and job seniority,
employment type, and workplace arrangement fields. Salary and benefits remain
non-blocking. Trusted LinkedIn job-detail URLs are accepted when the record is
not explicitly Easy Apply-only; explicit Easy Apply-only records remain
blocked. The tests add fixtures for those decisions and for audit field-group
and URL-policy impact reporting, with the audit implementation still pending.

### Verification

- Initial project interpreter check before worktree creation: Python `3.12.7`.
- RED: the project interpreter ran `tests/test_job_publication_completeness.py`; `41 passed, 9 failed` for the new behavior, as expected.
- GREEN for the validator changes: the project interpreter ran the focused test; `50 passed`.
- Subsequent audit-test verification was blocked because the project `.venv`
  directory became empty and no `Scripts\\python.exe` remained. The required
  project interpreter could not be invoked; no global interpreter was used and
  no packages were installed.
- `git diff --check`: passed before the implementation commit.

### Acceptance status

- Required field groups and reason-code behavior: implemented but not fully
  re-verified after the audit-test additions.
- Salary/benefits non-blocking and five owner-marked fields blocking: covered
  by focused tests from the RED/GREEN cycle.
- Full audit output, publisher policy wiring, contract documentation, and final
  acceptance verification: not completed.

### Result

Blocked by the missing required Python environment. Linear was left at the
blocked lifecycle rather than `In Review`; no deployment, merge, or cleanup was
performed.

## Attempt 02 — implementation resumed after environment restoration

- Issue: `RUN-31` / `[T32] Define and enforce the minimum publishable job and company contract`
- Baseline SHA: `405c70e7dbc3bc4853d39a46b88358e5d23c4f45`
- Branch: `feat/t32-publication-completeness-contract`
- Worktree: `C:\Users\ahmed\Projects_Local\job-automation\runr-t32-publication-completeness-contract`
- Implementation commit: `f81a264634f3dcb8b2ae70b8b51ccee091678261` (`feat(t32): enforce publication completeness contract`)
- Metadata commit: pending (this attempt log is committed separately after the implementation commit)
- Chat link: unavailable in the API session; Linear issue: https://linear.app/runr/issue/RUN-31/t32-define-and-enforce-the-minimum-publishable-job-and-company

### Scope changed

- `backend/acquisition/job_publication_completeness.py`
- `scripts/audit_job_publication_completeness.py`
- `scripts/publish_producer_states.py`
- `tests/test_job_publication_completeness.py`
- `docs/JOB_PUBLICATION_COMPLETENESS_CONTRACT.md`

The restored implementation enforces the owner decision that company logo,
company enrichment, seniority, employment type, and workplace arrangement are
blocking publication requirements. Salary and benefits remain non-blocking.
The validator accepts a trusted LinkedIn job-detail URL only for a LinkedIn
record that is not explicitly Easy Apply-only; employer listing/detail
fallbacks remain rejected. The offline audit now reports required-field
coverage by job/company group and the eligibility delta for strict rejection
of trusted LinkedIn detail URLs. The incremental producer publisher now uses
`publication_policy_v2`.

### Verification

- Restored `.venv\Scripts\python.exe` from the installed Python 3.12.7
  interpreter and installed `requirements-dev.txt` plus the editable
  `runr_automation` package. The project interpreter reports `Python 3.12.7`.
- TDD RED/GREEN sequence completed; final T32 contract test:
  `.venv\Scripts\python.exe -m pytest tests/test_job_publication_completeness.py -q`
  → **53 passed**.
- Combined focused source/audit run: **65 passed, 3 failed**. The three
  failures are legacy audit fixtures that omit the newly blocking owner-marked
  fields; they are outside the exact allowed implementation/test paths and are
  recorded rather than bypassed.
- Full repository run: **1856 passed, 16 failed, 91 subtests passed**. Six
  failures exercise legacy records missing the new blocking fields; the
  remaining failures are in unrelated backend, route, state-schema, runtime,
  and tracker areas outside the allowed paths.
- Ruff check on all changed Python files: passed.
- Both audit CLI `--help` checks: passed.
- Relative Markdown-link check: passed.
- `git diff --check`: passed.

### Acceptance status

- Required job/company fields and owner-marked blocking behavior: implemented
  and covered by the focused suite.
- Salary/benefits non-blocking: covered by the focused suite.
- Audit field coverage and URL-policy impact reporting: implemented and
  covered by the focused suite.
- Runtime publisher wiring: implemented and covered by the focused suite.
- No deployment, merge, or cleanup performed.

### Result

Implementation is ready for Linear review. The worktree remains isolated on
the feature branch; the next lifecycle state is `In Review`, not `Done`.
