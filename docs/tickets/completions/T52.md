# T52 / RUN-52 provider matrix completion evidence

The current [capability matrix](../../assisted-apply/simplify-parity-matrix.md) covers Greenhouse, Lever, Ashby, Avature, BambooHR, iCIMS, Jobvite, Recruitee, SmartRecruiters, Taleo, Workday, and unsupported/generic forms. Each row distinguishes detection/fill, upload, intermediate navigation, terminal behavior, and the level of fixture evidence. The [architecture assessment](../../assisted-apply/architecture-assessment.md) reconciles the earlier recovered proposal with integrated T47–T51 boundaries. No live-provider or Web Store claim is made.

Implementation base: permanent predeployment `c252e6d108dc0ab0f090ad44c7ee2ddc9c2a9f3f`. The exact implementation revision and protected integration evidence are recorded on Linear RUN-52.

## Focused verification

- `npm --prefix apps/browser-extension run check:all`: passed; 34 unit files / 367 tests and 32 Chromium browser tests; production manifest and never-submit boundary checks passed.
- `npm --prefix apps/browser-extension run check:edge`: passed; Edge build, manifest and never-submit boundary checks passed.
- `npm --prefix apps/browser-extension run test:e2e:edge`: passed; 7 Edge browser tests.
- `.venv\Scripts\python.exe -m pytest -q tests/test_backend_api.py -k assisted_apply`: passed; 2 tests, 120 deselected; interpreter 3.12.7.

The Edge suite covers Greenhouse/Lever and common extension flows, but does not run the Avature panel fixture; the matrix marks that path unverified in Edge. Historical AA-226 live pilot remains 0/10. Generic upload and multi-step provider coverage remain bounded follow-up work.
