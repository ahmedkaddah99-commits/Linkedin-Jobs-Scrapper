# RUN-40 / T41 completion evidence

Date: 2026-09-20

## Delivered

- Added a CompanyEnrich adapter for the documented domain GET and property POST contracts with Bearer authentication and `waitForEnrichment=false`.
- Dynamically discovers `Company_Enrich_API_KEY` and every non-empty numeric `Company_Enrich_API_URL_vN` slot without a version cap; slot values are treated as credentials and never logged.
- Added deterministic credential rotation/failover, per-credential request/credit/header accounting, request and credit budgets, caller-bounded concurrency, runtime `x-ratelimit-*` pacing/backoff, circuit breaking, and no retry after ambiguous timeouts.
- Added domain-first resolution, deduplicated batch POSTs in chunks of at most 50, per-domain credit accounting, property fallback, stale-cache fallback, and no duplicate single-company request after successful batch resolution.
- Maps only the approved identity/publication fields. Identity metadata is stored through the repository’s `additional_fields` path, with provenance, confidence, lookup method, and retrieval timestamps; stronger existing first-party identity is preserved.
- Does not request workforce expansion, persist the provider payload wholesale, or fabricate career URLs.
- Added a dry-run service mode that reports contract-passing companies without claims, profile writes, logo storage, or attempt records.

## Fixture coverage

Offline tests cover successful domain/property responses, batch success and 50-domain chunking, no-match, ambiguous timeout, 429/rate-limit, stale cache, 401/402/422/5xx provider errors, credential failover, redaction, identity precedence, and non-mutating dry-run behavior.

## Verification evidence

Worktree: `C:\Users\ahmed\Projects_Local\job-automation\runr-t41-companyenrich-employer-resolution`

- Python: `3.12.7`
- Focused command:

  `.venv\Scripts\python.exe -m pytest -q tests/test_phase_f_company_enrichment.py tests/test_company_identity_canonicalization.py tests/test_log_privacy.py`

  Result: `45 passed in 9.36s`

- Supplemental lint:

  `.venv\Scripts\python.exe -m ruff check backend/application/company_enrichment.py backend/integrations/companyenrich.py tests/test_phase_f_company_enrichment.py tests/test_company_identity_canonicalization.py tests/test_log_privacy.py`

  Result: `All checks passed!`

Implementation commits:

- `4b5bef35c486de8903afaa8ffeb481dde0b5f73c`
- `b884b5278773bb8edc9859cbeee634aef2cc35f9`

No live CompanyEnrich calls or real credentials were used. Shared checkout remains untouched. Handoff target is `In Review`; merge, deployment, and `Done` are explicitly out of scope.
