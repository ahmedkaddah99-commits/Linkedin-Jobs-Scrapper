# T44 (RUN-44) completion evidence

**Outcome:** Every recovered acquisition asset from the 2026-09-19 recovery set is preserved off-host under `runr/acquisition/checkpoints/recovery/2026-09-19/` with HEAD- and download-verified byte sizes and SHA-256 hashes, a versioned preservation manifest, a systemd-owned recurring backup schedule, an executed isolated restore drill, and a documented operator runbook. No active VPS state was touched and no production restore was performed.

## What changed (implementation SHA `9fa3fc62386007a6d75c5accc78c8936a769f9e2`)

- `scripts/acquisition_state_backup.py`: `preserve-recovery-set` (manifest-driven immutable upload of every recovered asset), `verify-recovery-set` (HEAD + optional isolated download + SQLite integrity/schema/row-count + `git bundle verify`), `scheduled-backup` (create + upload-before-manifest-commit + receipt-gated prune).
- `deploy/systemd/runr-acquisition-backup.service` + `.timer`: hardened daily 05:00 off-host backup of the LinkedIn and employer producer state (installed and enabled by `deploy/setup.sh`).
- Contract, env example, WS-7 doc, WS-3/WS-5 data doc, `docs/subsystems.yaml` updated; **WS7-G11 closed** in `docs/reverse-engineering/02-deployment/vps-runtime-and-acquisition-timers.md` §10 (the registry `known-gaps.md` does not carry Phase-2 `WS<n>-G<k>` rows by its own merge rule, so the closure lives in the owning doc).
- Evidence: `data/audit/runr_source_state/2026-09-19/offhost_preservation_manifest.json`, `preservation-receipt.json`, `offhost_preservation_verification.json`, `offhost_preservation_runbook.md`.

## Verified facts

- 13 files / 4,915,180,506 bytes uploaded to R2; 13/13 remote HEAD + isolated download verifications match local sizes and SHA-256 hashes.
- Drill re-verified from isolated downloads: LinkedIn producer DB integrity ok with `jobs` 188,206 and `job_company_observations` 188,206; employer DB `companies` 428 / `jobs` 2,612; enrichment DB `url_resolution` 15,454; Git bundle verifies with complete history and the preserved refs (`deployment/render-turso-r2` `30c8d5f1`, `temp/runr-linkedin-final` `e8469711`, `temp/runr-employer-final` `6ea7f460`, `temp/runr-production-final` `58a96674`).
- `company_sources_linkedin_ids.csv` (17,601 rows) preserved with its recorded 12,059 resolved LinkedIn company IDs; verified by byte size + SHA-256 (CSV content not re-parsed in the drill).
- Required tests: 26 passed; 1 pre-existing baseline failure (`tests/test_rc023_vps_runtime.py:99`, documented WS7-G1 stale assertion, owned by the C5/C6 candidate).

## Residual scope handed to reviewers

- `systemd-analyze verify` of the new units must be run on a systemd host during review/deployment (Windows worktree).
- Host activation of `runr-acquisition-backup.timer` is recorded in `setup.sh` but the VPS itself is U2 (host state) scope.
- Production restore into active VPS state is explicitly out of this ticket (RUN-45/T45).
