# T49 (RUN-49) completion evidence

**Outcome:** The approved profile-package API and extension contract are implemented on the isolated T49 branch. The backend exposes a strict, authenticated, Runr Pro-only profile package; the service worker keeps session tokens private and validates the response; the shared message package and panel mapper consume only approved, provenance-bearing facts. No merge, deployment, manifest permission, store change, or new profile-package persistence was performed.

**Implementation commits:** `7cf3da7b` (`feat(T49): add approved profile package contract`) and `c50077b9` (`fix(T49): reject unapproved profile facts`)

## What changed

- Added `POST /v1/assisted-apply/extension/profile-package` with exact extension-origin session authentication, Runr Pro gating, an empty strict request body, and schema-versioned approved sections built from `_profile_package_sections`.
- Added the typed `ProfilePackagePayload` wire contract and strict runtime validation. Candidate approval, answer review state, experience bullet approval, education confirmation, and skill/language confirmation must all be approved before mapping.
- Added the service-worker `ASSISTED_APPLY_PANEL_PROFILE` path with sender/frame checks, service-worker-only session tokens, and generic fail-closed malformed-response handling.
- Added the provider-neutral `CandidateProfile` mapper using `proposed_value`, approved bullets, and conservative ISO year-month parsing.
- Added backend, wire-validator, mapper, wrong-origin, Pro-gating, and malformed-body tests; updated backend API and Assisted Apply subsystem documentation.

## Verification

- `.venv\Scripts\python.exe --version`: Python 3.12.7.
- `.venv\Scripts\python.exe -m pytest -q tests/test_backend_api.py -k "assisted_apply or extension_origin"`: **4 passed, 4 subtests passed**.
- `npm --prefix apps/browser-extension run test:unit`: **33 test files, 339 tests passed**.
- `npm --prefix apps/browser-extension run typecheck`: **passed**.
- `npm --prefix apps/browser-extension run build`: **passed**.
- `npm --prefix apps/browser-extension run verify:manifest`: **passed**.
- `npm --prefix apps/browser-extension run verify:assisted-apply-boundary`: **passed**.
- Focused reviewer re-check of HEAD `c50077b9`: **no remaining Critical or Important findings**.

## Scope and handoff

- Starting revision: `0ec76fbdfe4948488bd40a5c7b43a0db4a67a809`.
- Isolated worktree: `C:\Users\ahmed\AppData\Local\RunrAutomation\worktrees\run-49`.
- Scope manifest hash: `7dfd6ba8e217462301404de8d9c291de213e9306d1493db217baf71917f4c164`.
- No committed `docs/tickets/attempts/T49.md` was created; attempt evidence is recorded on the Linear RUN-49 issue.
- Branch remains available for review. Production merge/deployment is outside this implementation handoff.
