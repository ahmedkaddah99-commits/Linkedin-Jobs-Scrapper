# T46 research evidence — Complete provider-neutral Assisted Apply autofill safely

- **Parent issue:** RUN-46 / T46
- **Primary subsystem:** `assisted-apply-extension` (WS-9)
- **Research baseline:** `deployment/render-turso-r2` / `origin/deployment/render-turso-r2` at `30c8d5f1641951148de3e30f44165e8e69fb764e`
- **Recovered source:** `feature/admin-analytics-final-production` at `ce3718b0`; scoped source commit `0d7f2b5cca64a2c3fa0d5efcf0a357fb574b04c3`, parent `0c4c4791a710c4575299d65923b03aca0e466721`
- **Date:** 2026-09-20. Internal repository and Linear research only; no live provider, production, or customer-data access.
- **Input fingerprint:** `sha256:054f728bbfc5b84f01d743a6f6088bde6c322df1941cdf64f73a0af65a2f42d9` (baseline/source blobs for the 48 scoped paths, active implementation branch heads, and predeployment-ref state)

## Research State: Current — ticket creation can proceed; only the safety-gate child can start immediately

## Verified source and scope

- Commit `0d7f2b5c` still exists locally and is contained by local branch `feature/admin-analytics-final-production`.
- The Assisted Apply subset is exactly 48 paths: 34 under `apps/browser-extension`, 11 under `packages`, one backend route, and two Assisted Apply documents.
- `git diff 0d7f2b5c^ 0d7f2b5c -- <48-path scope> | git apply --check --verbose` passes against `30c8d5f1`; `git diff --check` also passes.
- There is no scoped source drift from `0d7f2b5c^` to the current deployment baseline. The only nearby drift is `.github/workflows/ci.yml`, which is not in the recovered 48-path patch and must be integrated deliberately.
- The whole commit must never be cherry-picked or merged. It also contains acquisition data, scripts, frontend/admin residue, archives, and provider work. A two-way merge simulation reports conflicts in backend, frontend, scripts, and tests. Recovery must be path-scoped and reviewed.

## Reusable capabilities and missing safety work

- Provider-neutral inspection/planning exists in recovered `packages/ats-core/src/{application-context,field-intent,generic-inspector,generic-planner,generic-upload,question-model,resume-match}.ts` with unit fixtures for Ashby, Avature, BambooHR, iCIMS, Jobvite, Recruitee, SmartRecruiters, Taleo, and Workday.
- The recovered in-page assistant exists in `apps/browser-extension/entrypoints/assistant-panel.tsx` and `apps/browser-extension/src/panel/**`, with runtime registration and a profile-package flow.
- The recovered backend endpoint in `backend/api/routes/assisted_apply_packages.py` reuses approved profile sections and adds a session-authenticated POST profile-package route; it creates no new profile source of truth.
- The current never-submit gate is incomplete for this recovered code. `apps/browser-extension/scripts/verify-assisted-apply-boundary.mjs` does not scan the panel or entrypoints and does not detect synthetic MouseEvent activation.
- Recovered `assistant-panel.tsx:180-195` authorizes and dispatches a synthetic click. Recovered `step-navigation.ts:113-160` classifies controls from label text only. A terminal control labelled only “Next” or “Continue” can therefore be misclassified.
- Recovered unit tests prove the panel-side guard blocks obvious terminal labels, but the recovered E2E at `aa302-auto-panel.spec.ts:172-197` only checks that Continue is displayed and that the final Submit control is hidden. It does not operate Continue and verify the transition.
- Existing `installSubmissionGuard` may cancel the recovered panel's authorized synthetic click depending on listener coexistence/order. This needs a behavioral integration test before navigation can be accepted.
- Recovered `packages/ats-core/src/declarative-actions.ts:453-565` adds click-based enhanced-select and repeat-row behavior. These are not terminal navigation, but must be covered by the expanded static policy and runtime tests.

## Deduplication result: clear for a current successor hierarchy

- RUN-6 / T03 covered the original 48-path review and RUN-22 / T23 covered boundary hardening. Both were canceled on 2026-09-18 and reference baseline `58a96674`.
- The new work is not a title-only recreation: it uses baseline `30c8d5f1`, preserves the canceled issues as provenance, separates the unsafe monolith into dependency-ordered children, adds current merge evidence, and makes the safety gate the first executable unit.
- RUN-6 and RUN-22 must be related to the new parent/children; they must not be reopened, closed again, or treated as implementation branches.

## Active-ticket and predeployment compatibility

- Dry merge simulation against `origin/deployment/render-turso-r2` succeeded for the active implementation branches for T26, T28, T29, T32, T36, T39, T40, T43, and T44.
- None of those branches changes the proposed extension, shared-package, or profile-package API paths. T44 alone overlaps the optional completion path `docs/subsystems.yaml`; the Assisted Apply work must leave that registry unchanged unless ownership or invariants genuinely change, and any necessary registry edit must be serialized after T44.
- No local or remote branch matching `predeployment`, `pre-deployment`, or `predeploy` exists after fetching origin. Ticket branches can be created and verified now, but final promotion to the permanent predeployment branch is externally blocked until that protected branch exists or its canonical name is supplied.

## Versioned execution plan

- **Plan:** `AA-COMPLETE-2026-09-20-v1`
- **Wave 1:** T47 — harden and wire the never-submit safety gate. This is the only child eligible to start immediately.
- **Wave 2:** T48 — recover provider-neutral ATS core and declarative field execution; blocked by T47.
- **Wave 3:** T49 — add the approved profile-package API/message/background contract; blocked by T48.
- **Wave 4:** T50 — integrate broad-host runtime registration and the in-page autofill panel without automated step activation; blocked by T48 and T49.
- **Wave 5:** T51 — implement and prove safe intermediate-step navigation; blocked by T47 and T50.
- **Wave 6:** T52 — run the provider/browser capability matrix and close completion evidence; blocked by T48–T51.

All six children are compatible with the active non-WS-9 implementation branches because their source paths and shared conflict resources do not overlap. The six children are intentionally serialized with one another where contracts or files overlap.

## Implementation-start decision

Implementation can start for T47 after Linear creation verifies exactly one `Issue Status: Ready`, one `Issue Type: Improvement`, the WS-9 subsystem label, the parent relation, the current plan, and the hashed scope manifest. T48–T52 must remain `Waiting for Predecessor` until their declared blockers reach the required state. The parent is an umbrella and must not receive an implementation worktree.
