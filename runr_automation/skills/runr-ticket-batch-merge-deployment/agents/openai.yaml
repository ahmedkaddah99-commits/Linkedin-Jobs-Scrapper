interface:
  display_name: Runr Batch Deployment
  short_description: Promote selected tested Runr issues to deployment
  default_prompt: Use the Runr batch deployment merge skill for these issues.
policy:
  allow_implicit_invocation: false
